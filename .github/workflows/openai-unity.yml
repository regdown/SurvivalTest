name: OpenAI Unity Automation

on:
  workflow_dispatch:
    inputs:
      task:
        description: 'Описание задачи для GPT'
        required: true
        type: string

jobs:
  automate-task:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Request GPT and apply file changes
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          pip install openai
          mkdir -p Assets/Scripts
          python - <<EOF
          import os
          import re
          import openai

          openai.api_key = os.getenv('OPENAI_API_KEY')
          task = """${{ github.event.inputs.task }}"""

          system_prompt = (
              "Ты — помощник Unity-разработчика. "
              "Если задача требует создать или изменить код, возвращай его в формате:\n\n"
              "[FILE: путь/к/файлу.cs]\n<код файла>\n\n"
              "Только указывай нужные файлы, без пояснений и описаний. Не добавляй Markdown (например, ```csharp)."
          )

          response = openai.ChatCompletion.create(
              model="gpt-3.5-turbo",
              messages=[
                  {"role": "system", "content": system_prompt},
                  {"role": "user", "content": task}
              ]
          )

          content = response['choices'][0]['message']['content']

          with open("GPT_Response.md", "w", encoding="utf-8") as f:
              f.write(content)

          # Разбор ответа GPT и запись файлов
          matches = re.findall(r'\[FILE: (.*?)\]\n(.+?)(?=\n\[FILE: |\Z)', content, flags=re.DOTALL)
          for filepath, code in matches:
              full_path = os.path.join(filepath.strip())
              os.makedirs(os.path.dirname(full_path), exist_ok=True)
              with open(full_path, "w", encoding="utf-8") as f:
                  f.write(code.strip())

          EOF

      - name: Commit and create PR
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: auto/openai-changes
          title: "🤖 GPT Automated code changes"
          commit-message: "🛠️ GPT внёс автоматические изменения в Unity-проект"
          body: |
            GPT автоматически внёс изменения в следующие файлы:

            - Задача: ${{ github.event.inputs.task }}
            - Подробный ответ: см. файл [GPT_Response.md]

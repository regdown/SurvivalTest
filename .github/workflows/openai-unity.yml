name: OpenAI Unity Automation

on:
  workflow_dispatch:
    inputs:
      task:
        description: 'Описание задачи для GPT'
        required: true
        type: string

jobs:
  automate-task:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Request GPT response
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          pip install openai
          python - <<EOF
          import os
          from openai import OpenAI

          api_key = os.getenv('OPENAI_API_KEY')
          task = """${{ github.event.inputs.task }}"""

          client = OpenAI(api_key=api_key)

          response = client.chat.completions.create(
              model="gpt-4o",
              messages=[
                  {"role": "system", "content": "Ты — помощник по Unity и программированию. Тебя попросят внести изменения в проект Unity. Дай подробные инструкции и, при необходимости, код."},
                  {"role": "user", "content": task}
              ]
          )

          answer = response.choices[0].message.content
          with open("GPT_Response.md", "w", encoding="utf-8") as f:
              f.write(answer)
          EOF

      - name: Commit and create PR
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: auto/openai-changes
          title: "🤖 GPT Automated changes"
          commit-message: "🛠️ Автоматические изменения от GPT"
          body: |
            Автоматически созданные изменения с помощью GPT:

            - Задача: ${{ github.event.inputs.task }}

            Подробнее смотри в [GPT_Response.md].
